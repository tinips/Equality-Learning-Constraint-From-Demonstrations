# Assets

This folder contains a curated set of lightweight figures for the GitHub README and documentation.

The complete generated experiment outputs are not tracked in Git. They should stay outside the repository or be published separately through an archive service if needed.
